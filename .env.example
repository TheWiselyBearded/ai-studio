# AI Studio environment — copy to .env and fill in.
# .env is gitignored; never commit it.

# Gemini (used by VideoGeneratorWindow when provider=Veo)
GEMINI_API_KEY=

# RunwayML (used by VideoGeneratorWindow when provider=Runway)
RUNWAYML_API_SECRET=
